global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "scrapper"
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/scrapper.yaml
        refresh_interval: 15s
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
